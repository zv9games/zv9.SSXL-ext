/// 🧭 SyncBridge — engine-side bridge for coordinating chunk delivery and diagnostics.
#[derive(Default)]
pub struct SyncBridge {
    // 🧪 Future fields:
    // pub tick_count: u64,
    // pub last_delivery_time: Option<std::time::Instant>,
    // pub diagnostics: Option<EngineDiagnostics>,
}
